name: dart3_sample
description: Flutter Sampple using Dart3
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0-191.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  google_fonts: ^4.0.3

  # Riverpod
  riverpod: ^2.1.3
  flutter_riverpod: ^2.1.3

  # go_router
  go_router: ^6.0.2

  # Drift
  drift: ^2.5.0
  sqlite3_flutter_libs: ^0.5.12
  path_provider: ^2.0.12
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

  # Drift
  drift_dev: ^2.5.2
  build_runner: ^2.3.3

flutter:
  uses-material-design: true
